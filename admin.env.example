# Copia a /data/admin.env en el volumen eyed-bio-uploads (ruta en el contenedor: /data/admin.env)
# O define estas variables en Portainer en el servicio eyed-bio (no solo nginx).

ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=cambia-esta-contraseña-segura
ADMIN_USERNAME=admin
