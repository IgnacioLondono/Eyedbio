# Expone PostgreSQL en localhost para desarrollo local.
# Uso: docker compose -f docker/compose/docker-compose.yml -f docker/compose/docker-compose.dev.yml up postgres -d
services:
  postgres:
    ports:
      - "5432:5432"
